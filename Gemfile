# Bundler
#   Gemfile
#     has correct syntax
#     should specify rubygems as a source using the SSL protocol on the first line
#     should list the hashie gem without specifying a version (FAILED - 1)
#     should list the sinatra gem with the specific version 1.4.4
#     should list the octokit gem specifying version 2.0 with a twiddle-wakka (FAILED - 2)
#     should list the awesome_print gem specifying a remote git repository (use github)

source "https://rubygems.org"
gem "sinatra", '1.4.4'
gem "rspec"
gem 'octokit', '~> 2.0'
gem 'hashie'



group :developer do
  gem "pry"
end
